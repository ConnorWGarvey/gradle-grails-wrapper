buildscript {
  repositories {
    mavenLocal()
    mavenCentral()
  }
  dependencies {
    classpath(
      'com.connorgarvey.gradle:gradle-grails-wrapper:0.4.2',
    )
  }
}

apply plugin: 'grails'

grails {
  version '2.0.3'
}

task wrapper(type: Wrapper) {
  gradleVersion = '1.0-milestone-9'
}
